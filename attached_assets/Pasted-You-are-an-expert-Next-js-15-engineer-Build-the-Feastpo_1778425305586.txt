You are an expert Next.js 15 engineer. Build the Feastpot admin panel at apps/admin.

STEP 1 — Scaffold apps/admin:
Run: cd apps/admin && npx create-next-app@latest . --typescript --tailwind --app --src-dir --skip-git
apps/admin/package.json name: "@feastpot/admin"

Middleware: redirect to /sign-in unless role is admin, support, finance, or compliance.

LAYOUT (apps/admin/src/app/layout.tsx):
Desktop sidebar navigation with links to all sections.
Sidebar items: Dashboard, Vendors, Disputes, Payouts, Compliance, Audit Log, Settings.
Show current user name and role at bottom of sidebar.
No mobile bottom nav — admin panel is desktop-first.

VENDOR APPROVAL QUEUE (apps/admin/src/app/vendors/page.tsx):
Table with tabs: All, Pending (default), Live, Suspended, Removed
Columns: Business name, Cuisine, Date applied, Documents status, FSA rating, Actions
Document status shows 5 icons (hygiene, insurance, ID, bank, kitchen) coloured: green=verified, amber=pending, red=rejected, grey=missing

VENDOR DETAIL PAGE (apps/admin/src/app/vendors/[id]/page.tsx):
Split layout:
Left: Vendor profile info (name, address, cuisine, status, rating, commission rate)
Right: Document review panel
  Each document: filename, upload date, expiry date, Download button (pre-signed URL), Verify button, Reject button (with reason input)
  Overall approval/rejection buttons at bottom: "Approve Vendor" (green), "Reject Application" (red, requires note)
After approval: vendor status changes to approved, email notification sent automatically

DISPUTE MANAGEMENT (apps/admin/src/app/disputes/page.tsx):
Table with filters: status (all/open/vendor_contacted/escalated/closed), severity (all/low/medium/high), assignedTo
Columns: Dispute ref, Order date, Customer, Vendor, Issue type, Severity badge, Status, Age (time since created), Assigned agent

DISPUTE DETAIL (apps/admin/src/app/disputes/[id]/page.tsx):
Three-column layout:
Col 1: Order summary (items, vendor, customer, total, payment intent ID)
Col 2: Timeline (status changes with timestamps and actor names), Vendor response text area (read-only), Customer description
Col 3: Resolution panel:
  Evidence gallery (photo grid with lightbox, document previews)
  Resolution type select (Full refund / Partial refund / Credit / Rejected / Escalated)
  Refund amount input (auto-populated from order total, editable for partial)
  Notes textarea
  Close dispute button

ANALYTICS DASHBOARD (apps/admin/src/app/page.tsx — home):
Metric cards: GMV this week, GMV this month, Active vendors, Orders today, Repeat order rate %, Avg basket size
Revenue chart (recharts LineChart): daily GMV last 30 days, x-axis dates, y-axis £
Vendor league table: top 10 vendors by GMV this month, columns: rank, name, GMV, orders, avg rating, dispute rate %

PAYOUT BATCH APPROVAL (apps/admin/src/app/payouts/page.tsx):
Show current week's batch (generated by Monday cron)
Table: Vendor name, Gross, Commission (shown as negative), Refund deductions, Net, Status, Hold toggle
Hold toggle: when toggled on, shows reason input inline
"Approve all" button at top (shows count of non-held payouts)
"Reconcile with Stripe" button: fetches Stripe balance transactions for the week, highlights any discrepancies

COMPLIANCE DASHBOARD (apps/admin/src/app/compliance/page.tsx):
Document expiry table sorted by urgency (days remaining ASC)
Red rows: expired, amber rows: expiring in <7 days, normal: 7-30 days
Columns: Vendor, Document type, Expiry date, Days remaining, Status, Action (re-verify button)

AUDIT LOG VIEWER (apps/admin/src/app/audit-log/page.tsx):
Filters: entity type, entity ID, actor user, action, date range
Table: timestamp, actor (name + role), entity type, entity ID (link), action, old status → new status
Export to CSV button

Output all pages and components in full.