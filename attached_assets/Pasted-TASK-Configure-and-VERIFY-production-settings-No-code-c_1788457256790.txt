TASK: Configure and VERIFY production settings. No code
changes. Configuration only.

The 2 September release audit found these absent. Setting a
variable is not the deliverable; proving it works is.

1. NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY
   Set in the CUSTOMER deployment environment, matching the
   same Stripe environment as the secret key. Verify by
   loading /checkout and confirming the "Payments aren't
   configured" message is gone and the card element renders.

2. ADMIN_REQUIRE_AAL2
   Set to true in BOTH the API and the admin app.
   Then enrol the admin account in TOTP using the existing
   Enable 2FA button on /settings.
   VERIFY: sign in with an AAL1 session and confirm it is
   rejected, not merely warned about.

3. SENTRY_DSN
   Set in production. VERIFY: trigger a deliberate test
   exception and confirm the event arrives in Sentry. The
   error page tells users "It's been logged"; make that true.

4. QUEUE_ALERT_SLACK_WEBHOOK_URL
   Set and VERIFY by triggering a test queue alert and
   confirming Slack delivery.
   Context: four compliance review-trigger jobs have been
   sitting failed since 18 and 19 August because nothing
   alerted. Investigate why they stalled, then retry or
   discard them deliberately and record the outcome.

5. GITHUB ACTIONS SECRETS AND VARIABLES
   Currently zero configured, so every credential-dependent
   check cannot run. Add isolated, non-production test
   credentials for the customer, vendor and admin E2E jobs
   and the financial suite.

6. BRANCH PROTECTION
   Open a PR so every new check reports at least once, then
   apply the committed protection payload. Then open a second
   PR that deliberately breaks a required check and confirm
   it cannot merge. Record the evidence.

7. MIGRATION LEDGER
   One historical rolled-back migration record remains in
   production. Either resolve it or document why it is
   benign. Do not mark a failed migration applied without
   confirming its database objects exist.

8. DOCUMENT EVERYTHING
   Add all required variables to .env.example, including the
   ones a prior audit found undocumented: ADMIN_REQUIRE_AAL2,
   CAPACITY_ENFORCEMENT, STRIPE_FEASTPASS_ANNUAL_PRICE_ID,
   STRIPE_FEASTPASS_MONTHLY_PRICE_ID, FINANCE_ALERT_EMAIL,
   CHARGEBACK_EVIDENCE_WARN_HOURS, QUEUE_ALERT_SUSTAINED_CHECKS,
   QUEUE_ALERT_REPEAT_MINUTES, IP_HASH_SALT, API_PROXY_TARGET.

ACCEPTANCE CRITERIA
- All four variables set AND verified by observed behaviour.
- A deliberately broken PR cannot merge.
- The four stalled jobs are resolved with a recorded outcome.
- Every required variable appears in .env.example.