TASK: Merge duplicated queues and apply consistent ageing.

M1. TWO DEAD-LETTER PAGES
"Dead-letter notifications" (outbox rows past retry) and
"Dead-letter Bull jobs" (failed queue jobs) are two top-level
entries for one operator concept. Merge into one "Dead
letters" page with two tabs. Shared: filter by queue or event
type, bulk retry, bulk discard, and an explanation of what
retry does. Preserve per-row Retry and Discard and the
logging of the acting user ID.
(The four stalled jobs sitting there are resolved in FP-01.)

M2. VENDORS AND APPLICATIONS
/vendors tabs: Pending, Live, Probation, Suspended, Removed,
All. /applications tabs: All, Pending, Under Review,
Information Requested, Approved, Rejected. Both contain
"pending" items. The real distinction (pre-account lead vs
post-account vendor) is invisible from the labels, so an
operator must check both to know the state of supply.
CHOOSE ONE, CONFIRM WITH ME FIRST:
  (a) Merge into one "Supply pipeline" with a single
      lifecycle: Applied, Under review, Info requested,
      Approved, Onboarding, Live, Probation, Suspended,
      Removed, Rejected.
  (b) Keep separate but rename to make the boundary obvious,
      cross-link each pending item to its counterpart, and
      show both counts on each page.
Option (a) is better for a solo operator.

M3. SLA AND AGEING EVERYWHERE
/catering shows a red "Overdue by 13d" badge per row. That
pattern is good. But /applications shows submission dates
(one from 18 June 2026, roughly ten weeks untouched) with NO
ageing indicator, and /disputes, /chargebacks, /reviews,
/menu-moderation and /payouts have none either.
Define an SLA per queue and render a consistent badge:
  Applications        respond within 2 business days
  Catering enquiries  1 business day (already done, keep)
  Disputes            per the vendor terms response window
  Chargebacks         driven by the Stripe evidence deadline;
                      the summary card already shows
                      "evidence due <72h", put it on the row
  Menu moderation     retrospective, lower urgency, show age
  Reviews             show age
  Payouts held        time against the 14-day maximum review
                      period stated in the terms
States: within SLA (neutral), approaching (warning), breached
(attention). USE THE EXISTING RED AND AMBER from the catering
page. Introduce no new colours.

M4. PAST-DATED WORK MUST LEAVE LIVE QUEUES
/catering lists an enquiry for an event dated 2026-08-21,
already passed, still NEW and still offering Assign and
Review. An operator cannot fulfil it. Add automatic handling:
when an event date passes without assignment, move to
"Expired", notify the customer it could not be fulfilled, and
keep it retrievable for analysis.

M5. CHASE AND BULK ACTIONS
Both current applications show FSA "Missing" in red so
neither can be approved, and there is no way to chase from
this screen. Add: a "Request information" action emailing the
applicant naming exactly what is missing and recording the
request; bulk select for chasing several; and a visible
record of when each was last chased so nobody is chased twice
in a week.

ACCEPTANCE CRITERIA
- One dead-letter destination, both tabs functional.
- No status value exists that is absent from the filters.
- Every operator queue shows a consistent ageing badge.
- A past-dated catering enquiry auto-expires and notifies.
- An application can be chased in one action, recorded.